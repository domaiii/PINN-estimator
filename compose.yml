services:
  python_pinn:
    container_name: pinn-container
    build: .
    image: python:3.14-slim
    volumes:
        - .:/app
    working_dir: /app
    tty: true            
    stdin_open: true    
    command: jupyter lab --ip=0.0.0.0 --port=8888 --no-browser --allow-root